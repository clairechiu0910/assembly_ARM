程式執行：
	先開input, output檔，再一個字一個字讀input檔，比較是不是a-z做運算，再放到memory裏面，在呼叫SWI放到output檔裏面輸出。
筆記：
	用參數設定讀檔、寫檔，再注意不要改到前面設定的東regieter的值，如果改到要記得再設定回去。在用SWI的時候，r0設定是讀檔、寫檔或是找檔案的長度，r1是SWI所需要的參數，有些回傳執會傳到r0, 有些回傳值（像READ）讀出的字元就會放到參數裏面，要在把他從參數裡拿出來放到register裏面在做運算。
